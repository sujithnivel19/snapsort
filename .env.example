# Backend (server/index.js) — Claude vision for pose classification.
# If unset, grouping falls back to a deterministic mock split.
ANTHROPIC_API_KEY=

# Frontend (Vite) — Google Drive import.
# If unset, the Drive import option shows as unconfigured.
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=
